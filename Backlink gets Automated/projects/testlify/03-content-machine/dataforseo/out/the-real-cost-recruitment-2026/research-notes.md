# Research notes — Cost Per Hire 2026 Refresh (agent commentary)

*Separate from the clean brief. What happened, calls made, what's weak, follow-ups.*

## Judgment calls
- Primary landed on `cost per hire` over `hard costs vs soft costs` (880 vol, KD 3) because the angle's spine is the stat refresh, not the breakdown — `hard costs vs soft costs` is folded in as a secondary anchoring its own section instead.
- `calculate cost per hire` folds in `calculating cost per hire` as a gerund duplicate — same query intent, no separate row.
- `how much do recruitment agencies charge` folds in the `recruiting/companies/does a` phrasing variants — treated as the same fee-benchmark question, not split out.
- Four in-body-only terms (`cost of hiring`, `costs of hiring`, `hiring cost`, `hiring costs`) sit below the volume/KD floor but are core to the angle's vocabulary — kept in-body, denied their own section per spec.
- Two spokes carved out (`how much do recruiters make per hire`, `cost per hire calculator`) rather than stretched into sections here — both are demand signals that don't fit this asset's angle (calculator = tool build, recruiter pay = different topic).
- `hard costs vs soft costs` needs a recruiting-specific frame in the actual write — it currently sits inside a much larger construction-terminology cluster at that volume/KD, so the section has to establish the HR context fast or it reads off-topic to search intent.

## Weak / uncertain
- Shortlist is thin: pool 735 → shortlist 61, an ~8% survival rate. Worth knowing the head term set here isn't deep — this is a narrow commercial cluster, not a broad one.
- SHRM (rank 3) is both a top-10 competitor and the exact source of the 2022 figure this asset is refreshing — direct citation and direct competition are the same page. The write phase needs to differentiate, not just cite.
- All three read-list winners (SHRM, Juicebox, Metaview) are strong, structurally complete pieces (6.3k–8.1k words) covering definition/formula/benchmarks/factors/reduction — this isn't a weak field to beat, it's a competent one with one specific structural gap (role/volume segmentation) to exploit.
- Juicebox and Metaview both returned exactly 15 headings on read — likely a parser cap, not the true page structure. Later sections on either page may be missing from what Step 5 saw, so the "gaps we can own" call is made on a partial read of those two pages.
- Related-searches surfaced real demand for "calculator" and "dashboard" — a tool/interactive format has legs here but is out of scope for this asset; noting it so it isn't lost.
- Featured snippet slot is open (none currently held) — a clean 40-60 word definition could win it, but this wasn't scored or verified as achievable, just flagged as available.

## Step 6
- No usable AEO signal this run — llm_mentions drifted completely off-topic (house-cleaning rates, financial-advisor fees) on every query tried; the underlying corpus has no coverage of hiring-cost topics. Standing recommendation: treat Step 6 as skip-by-default for narrow B2B/HR-analytical topics like this one until the corpus improves — the method added nothing here and cost a run for no signal.

## Follow-ups
1. Protect the two differentiators explicitly in the write: current-year figure as a standalone cited data point, and cost breakdown split by BOTH role and hiring volume — no winner does either.
2. Frame the `hard costs vs soft costs` section with explicit recruiting/HR context early (first sentence or heading), since the keyword's broader search cluster is construction terminology.
3. Handle SHRM carefully — it's the citation source for the 2022 figure AND a top-3 ranking competitor; cite it for the old number, then visibly supersede it with the current one, don't just parrot it.
4. Re-read Juicebox and Metaview in full during the write phase (not via the 15-heading capped extract) before finalizing the "gaps we can own" claim — confirm no role/volume segmentation exists past what Step 5 captured.
5. Route `cost per hire calculator` and `how much do recruiters make per hire` to `spoke-candidates.md` for future asset consideration — don't let them creep into this piece's scope.
6. Consider a 40-60 word definition block early in the piece to contest the open featured-snippet slot for `cost per hire`.
