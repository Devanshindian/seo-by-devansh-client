You are distilling the **brand scope** for {{BRAND}} — the single one-page reference that dozens of
parallel reasoning agents will read so they all point the same way when they turn competitor pages into
asset ideas.

It must be SHORT (about one page). A bloated scope is as bad as a rigid list — it slows every agent and
invites drift. Give direction, not a rulebook.

## What you are given

**The company record:**
{{COMPANY_RECORD}}

**Brand voice (who we are, who we serve, how we talk):**
{{BRAND_VOICE}}

**Features (what the product actually does):**
{{FEATURES}}

**Extra signal (stats we can cite / opinions we hold — use only where they sharpen the scope):**
{{EXTRA}}

## Write brand-scope.md with EXACTLY these four sections

### 1. What we are / who we serve
One short paragraph: the product, the buyer, and the job they hire it for. Plain and concrete.

### 2. Subjects we can credibly own
A handful of EXAMPLE themes drawn from the voice + features + record — written only to point agents in
the right direction. These are illustrative, NOT a checklist and NOT exhaustive. Make it explicit in the
text that if an agent reads a page and sees a strong on-brand subject not listed here, it should use it.
This is "here's the kind of thing we own," never "only these are allowed." (The moment it reads as a
strict allow-list, it recreates the rigid-list problem we are avoiding.)

### 3. Off-limits / not us
A few EXAMPLE subjects clearly outside this company's world — again directional signal, not a hard ban.
A listed off-limits cue is a strong hint to skip or transplant, not an automatic override of what the
page actually is.

### 4. Transplant note
State the rule plainly: when a competitor wins links on an off-brand subject but with a strong FORMAT,
keep the winning format and point it at our world; record the original off-brand subject in Notes, and
never let it become our subject.

## Output

Return ONLY the markdown for `brand-scope.md` — a heading, then the four sections. No preamble, no
sign-off, no code fences. About one page.
