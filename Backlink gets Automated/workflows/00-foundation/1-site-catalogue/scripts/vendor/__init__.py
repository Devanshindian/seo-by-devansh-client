# vendor/ — ONLY files we actually use, restructured to our shape, no dead code.
# Each vendored file carries a one-line header naming what it does and where the
# technique came from. Empty until a stage genuinely needs a vendored piece.
